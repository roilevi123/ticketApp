# Base config loaded for every test run. Activates the "test" profile so that
# application-test.properties (H2 in-memory DB) is used instead of production
# PostgreSQL, without requiring @ActiveProfiles("test") on individual test classes.
spring.profiles.active=test
